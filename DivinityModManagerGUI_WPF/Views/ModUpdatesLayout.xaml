<UserControl x:Class="DivinityModManager.Views.ModUpdatesLayout"
			xmlns:reactiveui="http://reactiveui.net"
			xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			xmlns:local="clr-namespace:DivinityModManager.Views"
			xmlns:controls="clr-namespace:DivinityModManager.Controls"
			xmlns:conv="clr-namespace:DivinityModManager.Converters"
			xmlns:vm="clr-namespace:DivinityModManager.ViewModels"
			xmlns:data="clr-namespace:DivinityModManager.Models;assembly=DivinityModManagerCore"
			xmlns:dd="urn:gong-wpf-dragdrop"
			xmlns:ag="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid"
			mc:Ignorable="d" 
			d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/DivinityModManager;component/Views/MainResourceDictionary.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Grid x:Name="UpdateGrid">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="1*" />
			<RowDefinition Height="10" />
			<RowDefinition Height="2*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock Text="Mod Updates" TextAlignment="Center" FontSize="24" Background="White" Grid.Row="0" />
		<ag:AutoGrid Visibility="{Binding NewAvailable, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue='Visible'}"
				RowCount="2" Rows="auto,*" Columns="*" Grid.Row="1"
			>
			<TextBlock Text="New Mods" TextAlignment="Center" FontSize="20" Background="White"/>
			<ListView ItemsSource="{Binding NewMods}" View="{DynamicResource NewModsGridView}" GridViewColumnHeader.Click="SortGridView_Click"/>
		</ag:AutoGrid>
		<GridSplitter Grid.Row="2" ResizeDirection="Rows" VerticalAlignment="Stretch" Background="Aqua" HorizontalAlignment="Stretch" />
		<ag:AutoGrid
				RowCount="2" Rows="auto,*" Columns="*" Grid.Row="3"
			>
			<TextBlock Text="Updates" TextAlignment="Center" FontSize="20" Background="White" />
			<ListView ItemsSource="{Binding Updates}" View="{DynamicResource ModUpdatesGridView}" GridViewColumnHeader.Click="SortGridView_Click"/>
		</ag:AutoGrid>
		<Grid Grid.Row="4">
			<StackPanel Orientation="Horizontal">
				<Button Content="Copy Selected to Mods Folder" />
			</StackPanel>
		</Grid>
	</Grid>
</UserControl>
